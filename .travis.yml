language: node_js
node_js:
  - 10
script: yarn build && yarn docs:build
deploy:
  - provider: npm
    skip_cleanup: true
    access: public
    src: ./dist
    # email:
    # api_key:
    on:
      tags: true
  - provider: surge
    skip_cleanup: true
    project: ./out
    # login:
    # token:
    # domain:
    on:
      tags: true
